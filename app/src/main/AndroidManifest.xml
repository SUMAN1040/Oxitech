<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/oxitech2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/oxitech2"
        android:supportsRtl="true"
        android:theme="@style/Theme.Oxitech"
        tools:targetApi="31">

        <activity
            android:name=".openui.SecondActivity"
            android:exported="false"
            android:theme="@style/Theme.Splash" />

        <activity
            android:name=".openui.FirstActivity"
            android:exported="false"
            android:theme="@style/Theme.Splash" />

        <activity
            android:name=".other.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>


<!--    https://chatgpt.com/share/67d860a0-3544-8012-b818-ea814b7a963b-->